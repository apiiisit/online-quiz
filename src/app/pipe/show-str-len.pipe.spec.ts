import { ShowStrLenPipe } from './show-str-len.pipe';

describe('ShowStrLenPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowStrLenPipe();
    expect(pipe).toBeTruthy();
  });
});
