import { RandomOrderPipe } from './random-order.pipe';

describe('RandomOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
